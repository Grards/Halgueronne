{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Ajout d'un nouvel utilisateur</h1>

   
    {{ form_start(form) }}
        {{form_widget(form) }}
        <button class="btn btn-primary">Créer le nouvel utilisateur</button>
    {{ form_end(form) }}

</div>
{% endblock %}