{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Modifier l'utilisateur</h1>
    </div>
    <div class="row">
        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button class="btn btn-success">Modifier l'utilisateur</button>
            <a href="{{ path('utilisateurs')}}" class="btn btn-danger">Retour</a>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}